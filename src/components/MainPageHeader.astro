---
export interface Props {
    isFull?: boolean;
}

const { isFull = false } = Astro.props;
---

<header class={`header ${isFull ? 'header-full' : ''}`}>
    {isFull && <h2>Официальный сайт</h2>}
    <a class="header-link" href="/">
        <h1>
            {isFull && <>ТСЖ </>}
            <span class="text-gradient">Магнолия 🌸</span>
        </h1>
    </a>
    {isFull && (
        <div class="links">
            <a href="mailto:kolpakova40-3@mail.ru">📬 kolpakova40-3@mail.ru</a>
            <a href="tel:+79151192536">☎️ +7 915 119-25-36</a>
        </div>
    )}
    <div class="links">
        <a href="/documents/ustav">Устав</a>
        <a href="tel:+79151192536">Отчеты</a>
        <a href="tel:+79151192536">Контакты</a>
    </div>
</header>
<style>
    h1 {
        font-size: 2.4rem;
        line-height: 0.9;
        font-weight: 800;
        margin: 0;
    }
    .header-full h1 {
        font-size: 3.55rem;
    }
    .text-gradient {
        background-image: var(--accent-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-size: 400%;
        background-position: 0%;
    }
    .instructions {
        line-height: 1.6;
        margin: 1rem 0;
        border: 1px solid rgba(var(--accent), 25%);
        background-color: white;
        padding: 1rem;
        border-radius: 0.4rem;
    }

    .links {
        margin-top: 15px;
        display: flex;
        gap: 0.2rem 1rem;
        flex-wrap: wrap;
    }
    .links > a {
        color: #444444;
        font-size: 1.15rem;
        white-space: nowrap;
    }

    .header-link {
        text-decoration: none;
        color: inherit;
    }
</style>
